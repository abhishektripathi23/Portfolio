const r = document.documentElement, t = document.getElementById('theme'), m = document.getElementById('menu'), mobile = document.getElementById('mobile'); const saved = localStorage.getItem('portfolio-theme'); setTheme(saved || (matchMedia('(prefers-color-scheme:dark)').matches ? 'dark' : 'light')); function setTheme(x) { r.dataset.theme = x; localStorage.setItem('portfolio-theme', x); t.innerHTML = x === 'dark' ? '☀' : '☾' } t.onclick = () => setTheme(r.dataset.theme === 'dark' ? 'light' : 'dark'); m.onclick = () => { mobile.style.display = mobile.style.display === 'block' ? 'none' : 'block' }; document.querySelectorAll('#mobile a').forEach(a => a.onclick = () => mobile.style.display = 'none'); const o = new IntersectionObserver(es => es.forEach(e => { if (e.isIntersecting) { e.target.classList.add('visible'); o.unobserve(e.target) } }), { threshold: .08 }); document.querySelectorAll('.reveal').forEach(e => o.observe(e)); document.getElementById('year').textContent = new Date().getFullYear(); const f = document.getElementById('contactForm'), s = document.getElementById('status'), b = document.getElementById('submit'); f.onsubmit = async e => { e.preventDefault(); if (!f.checkValidity()) { f.reportValidity(); return } b.disabled = true; b.textContent = 'Sending...'; try { const x = await fetch(f.action, { method: 'POST', body: new FormData(f), headers: { Accept: 'application/json' } }), d = await x.json(); if (!x.ok || !d.success) throw Error(d.message || 'Unable to send message.'); f.reset(); s.textContent = d.message; s.style.color = 'var(--success)' } catch (err) { s.textContent = err.message; s.style.color = '#dc2626' } finally { b.disabled = false; b.textContent = 'Send Message →' } };
